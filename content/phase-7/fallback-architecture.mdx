Fallback Architecture bảo vệ user experience khi model, provider, schema, network hoặc safety layer fail. Nhưng fallback không phải "thử model khác bừa". Fallback đúng là **controlled degradation**: giảm chất lượng/khả năng một cách có kiểm soát, vẫn giữ contract, security policy, cost budget và traceability.

---

## 1. The Incident (Tình huống sự cố)

<Callout type="danger" title="Báo cáo sự cố: Fallback sang model không support JSON, downstream parser crash, app trả 500 cho 3,000 user">
**Bối cảnh:** Một team build "AI extraction service" cho SaaS. Primary model trả JSON theo schema. Fallback policy: nếu primary fail, gọi model B. Model B không có structured output, trả text tự do.

**Sự cố:** Primary provider timeout 3 phút. Fallback tự động gọi model B. Model B trả text tự do "Dưới đây là dữ liệu..." thay vì JSON. Downstream parser crash vì không parse được. App trả 500 cho 3,000 user trong 3 phút.

1. Fallback không có schema-compatible check.
2. Fallback không có validation gate sau khi gọi.
3. Fallback chỉ đẩy lỗi xuống downstream parser.
4. Không có circuit breaker, nên fallback gọi model B 3,000 lần.
5. Không có fail-closed cho sensitive workflows.

**Hậu quả:** 3,000 user thấy 500 error trong 3 phút. Downstream parser crash. App phải tắt khẩn cấp. Team phải thiết kế lại fallback với validation gate, schema-compatible check, và fail-closed.
</Callout>

Bài học rút ra: fallback không được phá contract. Nếu primary model phải trả JSON theo schema nhưng fallback model trả text tự do, bạn không có fallback — bạn chỉ đẩy lỗi xuống downstream parser. Fallback phải giữ schema, safety, compliance, context và cost constraints.

---

## 2. Architecture Blueprint (Controlled Degradation)

Một fallback production không phải là "thử model khác". Nó là một **controlled degradation cascade**:

```text
[ Primary model ]
        │
        ▼
[ Success? ]
        │
        ▼
[ Validate response ]
        │
        ▼
[ Pass? → Return ]
        │
        ▼
[ Retry if retryable ]   ← bounded retry budget
        │
        ▼
[ Still fail? ]
        │
        ▼
[ Fallback model/path if policy allows ]   ← constraints
        │
        ▼
[ Validate again ]
        │
        ▼
[ Return or degraded response or fail closed ]
```

Key phrase: **if policy allows**. Không phải lỗi nào cũng fallback được. Không phải model nào cũng là fallback candidate.

---

## 3. Retry vs Fallback vs Degradation

| Mechanism | Purpose | Example | Risk |
|---|---|---|---|
| Retry | thử lại cùng provider/model | 503 retry | retry storm |
| Fallback | đổi provider/model/path | GPT → Claude | behavior/schema drift |
| Cache fallback | dùng answer cached | outage | stale answer |
| Degraded response | trả answer đơn giản hơn | no citations | lower quality |
| Fail closed | từ chối an toàn | sensitive data | UX friction |

<Callout type="warning" title="Fallback không được phá contract">
Nếu primary model phải trả JSON theo schema nhưng fallback model trả text tự do, bạn không có fallback — bạn chỉ đẩy lỗi xuống downstream parser.
</Callout>

---

## 4. Error Taxonomy

| Error | Retry? | Fallback? | Notes |
|---|---|---|---|
| 400 invalid request | no | no | fix code/payload |
| 401/403 auth | no | no | key/permission issue |
| 429 rate limit | yes | maybe | respect Retry-After |
| 408/timeout | yes | yes | bounded retry budget |
| 500/502/503/504 | yes | yes | provider unhealthy |
| context overflow | no | maybe | reduce context or long-context fallback |
| invalid JSON | repair/retry | maybe | schema-compatible fallback |
| tool args invalid | repair/retry | maybe | validate before execute |
| safety refusal | usually no | policy-dependent | do not bypass safety blindly |
| streaming interrupted | depends | difficult | partial output problem |
| budget exceeded | no | cheaper fallback | user tier policy |

<Callout type="danger" title="Safety refusal không phải lỗi kỹ thuật bình thường">
Fallback sang model khác để "né refusal" có thể là policy violation. Safety refusal cần route policy rõ, không được bypass mù quáng.
</Callout>

---

## 5. Task-Specific Fallback Cascades

### Reasoning task

```text
strong reasoning primary
  → strong reasoning secondary
  → ask clarification / human review
```

Không nên fallback xuống model yếu nếu answer high-stakes.

### JSON extraction

```text
schema-native model
  → repair loop
  → schema-reliable fallback
  → manual review / controlled error
```

### Sensitive data

```text
private/compliant model
  → private fallback
  → fail closed
```

Không fallback sang external provider nếu tenant policy không cho phép.

### RAG answer

```text
primary model
  → fallback model with same context
  → cached answer if source unchanged
  → say unavailable / ask user retry
```

---

## 6. Fallback Policy Constraints

Fallback candidate phải pass constraints:

- provider allowed by tenant policy,
- context window đủ,
- supports required output schema,
- supports required tools,
- cost within budget,
- latency acceptable,
- safety profile acceptable,
- prompt compatible enough,
- data region allowed.

```python
def fallback_allowed(candidate, req):
    return (
        candidate.allowed_for_tenant(req.tenant)
        and candidate.context_window >= req.required_context
        and (not req.requires_json or candidate.supports_json)
        and candidate.estimated_cost(req) <= req.max_cost
    )
```

---

## 7. Circuit Breaker Pattern

Circuit breaker ngăn app tiếp tục gọi provider đang lỗi.

States:

```text
Closed: provider healthy, calls pass normally
Open: provider unhealthy, fail fast / route fallback
Half-open: send limited probe calls to test recovery
```

Signals:

- consecutive failures,
- error rate over time window,
- timeout rate,
- p95 latency spike,
- 429 rate,
- invalid response rate.

Pseudocode:

```python
class CircuitBreaker:
    def __init__(self, failure_threshold=5):
        self.state = "closed"
        self.failures = 0
        self.failure_threshold = failure_threshold

    def allow_request(self):
        return self.state in {"closed", "half_open"}

    def record_success(self):
        self.failures = 0
        self.state = "closed"

    def record_failure(self):
        self.failures += 1
        if self.failures >= self.failure_threshold:
            self.state = "open"
```

---

## 8. Fallback and Streaming

Streaming khó fallback hơn non-streaming.

**Case 1: fail before first token.**

```text
safe to retry/fallback, user chưa thấy gì
```

**Case 2: fail after partial output.**

```text
user đã thấy một phần answer
fallback có thể duplicate/mâu thuẫn
```

Options:

- show partial + retry button,
- restart with notice,
- buffer output until validation for high-stakes tasks,
- avoid streaming for strict JSON extraction,
- use resumable protocol nếu provider hỗ trợ.

---

## 9. Fallback and Structured Output

Structured output cần validation gate sau mỗi attempt.

```text
primary output → JSON parse → schema validate → business validate
```

Nếu fail:

```text
repair prompt → retry → fallback schema-capable model → controlled error
```

Never return invalid JSON to app layer.

---

## 10. Cache Fallback

Cache fallback hữu ích khi provider down và answer không thay đổi nhanh.

**Safe-ish:**

- public FAQ,
- static policy with version,
- deterministic classification results.

**Dangerous:**

- personalized sensitive advice,
- financial/legal current data,
- tool-action workflows,
- stale permissions.

Cache fallback phải include:

```text
data_version + prompt_version + model + tenant/user permission version
```

---

## 11. Observability for Fallback

Trace fields:

```json
{
  "request_id": "req_123",
  "primary_model": "gpt-4o",
  "attempts": 2,
  "errors": ["timeout", "timeout"],
  "fallback_model": "claude-sonnet",
  "fallback_reason": "primary_timeout",
  "circuit_state": "half_open",
  "validation_status": "passed",
  "total_cost": 0.031,
  "user_visible_degradation": false
}
```

Metrics:

- fallback rate,
- fallback success rate,
- retry count,
- circuit open time,
- cost increase due to retries,
- validation failure rate after fallback.

---

## 12. Implementation Playbook: Fallback Manager with Circuit Breaker

```python
import random
from dataclasses import dataclass


@dataclass
class ProviderResult:
    ok: bool
    text: str | None = None
    error: str | None = None


class FakeProvider:
    def __init__(self, name, fail_rate=0.2, invalid_rate=0.1):
        self.name = name
        self.fail_rate = fail_rate
        self.invalid_rate = invalid_rate

    def call(self):
        r = random.random()
        if r < self.fail_rate:
            return ProviderResult(ok=False, error="timeout")
        if r < self.fail_rate + self.invalid_rate:
            return ProviderResult(ok=True, text="not json")
        return ProviderResult(ok=True, text='{"answer":"ok"}')


def validate_json_like(text):
    return text and text.strip().startswith("{")


def call_with_fallback(primary, fallback):
    trace = []

    first = primary.call()
    trace.append((primary.name, first.error or "ok"))
    if first.ok and validate_json_like(first.text):
        return first, trace

    second = fallback.call()
    trace.append((fallback.name, second.error or "ok"))
    if second.ok and validate_json_like(second.text):
        return second, trace

    return ProviderResult(ok=False, error="all_failed"), trace


primary = FakeProvider("primary", fail_rate=0.5)
fallback = FakeProvider("fallback", fail_rate=0.1)
result, trace = call_with_fallback(primary, fallback)
print(result, trace)
```

**Production checklist:**

- [ ] Có error taxonomy không?
- [ ] Retry và fallback có tách biệt không?
- [ ] Fallback có schema-compatible không?
- [ ] Fallback có data-policy-compatible không?
- [ ] Có circuit breaker không?
- [ ] Có xử lý streaming partial failure không?
- [ ] Có validation sau fallback không?
- [ ] Có trace fallback path không?
- [ ] Có cost cap cho retries/fallback không?
- [ ] Có fail closed cho sensitive workflows không?

---

## Post-Mortem & Troubleshooting (SRE/Gỡ lỗi)

<Callout type="warning" title="Pitfall 1: fallback sang model không support schema">
**Triệu chứng:** Nếu downstream cần JSON/schema, fallback model phải có khả năng tương đương hoặc bạn cần repair/validation gate. Nếu không, fallback chỉ đẩy lỗi xuống downstream parser.

**Fix:** Fallback candidate phải pass constraints: supports required output schema, supports required tools, cost within budget, latency acceptable.
</Callout>

<Callout type="warning" title="Pitfall 2: không log fallback path">
**Triệu chứng:** Khi quality giảm hoặc cost tăng, bạn cần biết request đã đi qua models nào và vì sao. Nếu chỉ log selected model, debug rất khó.

**Fix:** Trace fallback path phải log: primary, attempts, errors, fallback model, reason, circuit state, validation, cost, degradation.
</Callout>

<Callout type="danger" title="Pitfall 3: fallback bypass compliance hoặc safety">
**Triệu chứng:** Fallback không được gửi data sang provider bị cấm hoặc né safety refusal. Policy constraints phải áp dụng cho cả fallback.

**Fix:** Compliance constraints phải là hard constraints cho cả fallback. Safety refusal cần route policy rõ, không được bypass mù quáng.
</Callout>

---

## Cost / Latency / Reliability Unit Economics

| Quyết định | Lợi ích | Chi phí/rủi ro |
|---|---|---|
| Retry | thử lại cùng provider | retry storm, cost tăng |
| Fallback | đổi provider/model | behavior/schema drift |
| Cache fallback | dùng answer cached | stale answer |
| Degraded response | trả answer đơn giản hơn | lower quality |
| Fail closed | từ chối an toàn | UX friction |
| No circuit breaker | đơn giản | retry storm khi provider lỗi |
| No validation gate | đơn giản | invalid JSON đi ra app layer |
| No trace | đơn giản | debug không thể |

Fallback là nghệ thuật chuyển "lỗi" thành "controlled degradation". Mỗi fallback phải có validation gate, trace, cost cap, và policy constraints.

---

## Practice exercises

1. Viết fallback cascade cho RAG QA, JSON extraction, sensitive data.
2. Tạo retry/fallback matrix cho 12 error types.
3. Implement circuit breaker closed/open/half-open.
4. Simulate fallback cost increase với 1,000 requests.
5. Thiết kế user-visible message khi fallback degraded.
6. Viết trace schema cho fallback manager.
7. Phân tích safety refusal: khi nào fallback allowed vs blocked.
8. Viết validation gate cho JSON extraction fallback.

---

## Knowledge check

1. Retry khác fallback thế nào?
2. Controlled degradation nghĩa là gì?
3. Vì sao fallback phải preserve output contract?
4. Circuit breaker có những states nào?
5. Streaming fallback khó ở điểm nào?
6. Cache fallback nguy hiểm khi nào?
7. Safety refusal có nên fallback sang model khác không?
8. Trace fallback nên log gì?

---

## Answer key

1. Retry gọi lại cùng provider/model; fallback đổi model/provider/path.
2. Giảm khả năng/chất lượng một cách có kiểm soát, vẫn giữ policy và user-safe response.
3. Downstream parser/business logic phụ thuộc schema/contract; phá contract tạo lỗi muộn hơn.
4. Closed, open, half-open.
5. Nếu đã stream partial output, retry/fallback có thể duplicate hoặc mâu thuẫn với phần user đã thấy.
6. Khi data personalized, stale, permission thay đổi, hoặc high-stakes/current data.
7. Không được bypass mù quáng; chỉ theo policy rõ và safety constraints.
8. Primary, attempts, errors, fallback model, reason, circuit state, validation, cost, degradation.

---

## Điểm cần nhớ

- Fallback là controlled degradation, không phải thử model khác bừa.
- Retry, fallback, cache fallback, degraded response và fail closed là cơ chế khác nhau.
- Fallback phải giữ schema, safety, compliance, context và cost constraints.
- Circuit breaker bảo vệ hệ thống khỏi provider unhealthy.
- Streaming fallback cần xử lý partial output cẩn thận.
- Observability fallback path là bắt buộc để debug reliability và cost.

---

## Next chapter bridge

Routing và fallback quyết định model nào chạy và phục hồi khi lỗi. Nhưng agentic systems còn cần chuẩn hóa cách model truy cập tools, resources và prompts. Chương tiếp theo: **Model Context Protocol (MCP)** — protocol layer cho AI-tool connectivity.
